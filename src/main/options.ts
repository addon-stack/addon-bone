import {OptionsConfig, OptionsDefinition, OptionsProps} from "@typing/options";

export type {OptionsDefinition, OptionsProps, OptionsConfig};

export const defineOptions = (options: OptionsDefinition): OptionsDefinition => {
    return options;
};

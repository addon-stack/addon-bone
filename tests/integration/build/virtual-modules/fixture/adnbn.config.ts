import {defineConfig} from "adnbn";

export default defineConfig({
    name: "Virtual Modules Integration",
    description: "Generated entrypoint data and unused consumers.",
    jsFilename: "[name].js",
    iconSrcDir: "icons",
    iconOutDir: "icons",
});

import {defineConfig} from "adnbn";

export default defineConfig({
    name: "History Override Integration",
    description: "Chrome MV3 history page using the Vanilla view adapter.",
    version: "1.0.0",
    jsFilename: "[name].js",
    cssFilename: "[name].css",
});

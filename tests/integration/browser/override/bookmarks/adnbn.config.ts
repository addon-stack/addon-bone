import {defineConfig} from "adnbn";

export default defineConfig({
    name: "Bookmarks Override Integration",
    description: "Chrome MV3 bookmarks page using the Vanilla view adapter.",
    version: "1.0.0",
    jsFilename: "[name].js",
    cssFilename: "[name].css",
});

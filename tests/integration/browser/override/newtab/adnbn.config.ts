import {defineConfig} from "adnbn";

export default defineConfig({
    name: "New Tab Override Integration",
    description: "Chrome MV3 new tab page using the React view adapter.",
    version: "1.0.0",
    htmlDir: "ui",
    jsFilename: "[name].js",
    cssFilename: "[name].css",
});

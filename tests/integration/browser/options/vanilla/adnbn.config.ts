import {defineConfig} from "adnbn";

export default defineConfig({
    name: "Vanilla Options Integration",
    description: "Chrome MV3 options page using the Vanilla view adapter.",
    version: "1.0.0",
    jsFilename: "[name].js",
    cssFilename: "[name].css",
});

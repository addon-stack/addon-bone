import {defineConfig} from "adnbn";

export default defineConfig({
    name: "React Options Integration",
    description: "Chrome MV3 options page using the React view adapter.",
    version: "1.0.0",
    htmlDir: "ui",
    jsFilename: "[name].js",
    cssFilename: "[name].css",
});

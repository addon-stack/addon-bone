import {defineConfig} from "adnbn";

export default defineConfig({
    name: "View Render Integration",
    description: "Chrome MV3 views rendered by the injected Vanilla and React view builders.",
    version: "1.0.0",
    jsFilename: "[name].js",
});
